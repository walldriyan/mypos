// This page is intentionally left blank to bypass the login flow during development.
// The user is automatically logged in via a dummy session in AuthProvider.
export default function LoginPage() {
  return null;
}
